# Copy to seller_monitor.env and fill this value only when real PushPlus sending is approved.
# The seller monitor never reads the project's .env or FEISHU_WEBHOOK.
PUSHPLUS_TOKEN=your_pushplus_token_here

